# MFTT
MTFTT: Anemia Subtyping from Blood Tests using Transformers A deep learning model that classifies anemia into five categories from routine CBC (blood test) data. Each blood feature is tokenized and processed through a Transformer encoder that simultaneously performs subtype classification, binary screening, and hemoglobin prediction.
